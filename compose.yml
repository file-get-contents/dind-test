volumes:
  sources:

services:
  dind:
    container_name: dind
    build:
      context: .
      dockerfile: Dockerfile
      target: host
    privileged: true
    init: true
    tty: true
    cap_add:
      - ALL
    volumes:
      - sources:/home/dinder
#    extra_hosts:
#      - dev.yossoi.com=191.168.0.1
#    mem_limit: 1g
#    memswap_limit: 1g
#    storage_opt:
#      size: '5G'

